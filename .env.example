# Bot Telegram - obter em @BotFather
TELEGRAM_BOT_TOKEN=7810006820:AAHnNOZnMWLG756LyK0sGOsp_n-OS7ZHZnA

# OpenRouter - chat (LLM). Chave em https://openrouter.ai
OPENROUTER_API_KEY=
# Modelo (ex.: openai/gpt-4o-mini, anthropic/claude-3-haiku)
OPENROUTER_MODEL=openai/gpt-4o-mini

# OpenAI - só para Whisper (STT) e TTS (áudio)
OPENAI_API_KEY=

# Pasta do Google Drive com base de conhecimento (ID da pasta)
# Ex: https://drive.google.com/drive/folders/XXXXXXXX -> XXXXXXXX
DRIVE_FOLDER_ID=
# Para deploy em PaaS (Railway, Render): cole o conteúdo completo do token.json aqui (gerado uma vez no PC).
# GOOGLE_TOKEN_JSON={"refresh_token":"...","client_id":"...","client_secret":"...", ...}
# Nome da subpasta com imagens de filtros (ex.: bnbFiltros).
# DRIVE_FILTER_IMAGES_FOLDER=bnbFiltros

# Opcional: ElevenLabs para TTS (se não usar OpenAI TTS)
# ELEVENLABS_API_KEY=

# Banco: SQLite (padrão), PostgreSQL ou Supabase REST
# Opção 1 - Connection string Postgres (ex.: Supabase pooler). Senha com @ use %40.
# DATABASE_URL=postgresql://user:senha@host:6543/postgres
# Opção 2 - Supabase via REST (SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY)
# SUPABASE_URL=https://xxxxx.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=
# SQLite (quando DATABASE_URL e Supabase não definidos): DATABASE_PATH=.tmp/sdr_bot.db

# Message buffer (OPCIONAL) — NÃO é o banco. REDIS_URL = debounce (agrupar msgs). Redis Labs: use rediss:// se redis:// falhar.
# REDIS_URL=redis://localhost:6379/0
# MESSAGE_BUFFER_DEBOUNCE_SECONDS=5
# MESSAGE_BUFFER_TENANT_ID=default

# Modo produção: log em arquivo (.tmp/logs/bot.log) e nível INFO. Use com run_production.py
# RUN_MODE=production
# LOG_DIR=.tmp/logs

# Atraso antes de enviar a resposta (typing). 0 = desligado
# BOT_RESPONSE_DELAY_SECONDS=2
# Pausa entre várias mensagens (texto, fotos, áudio) em segundos
# BOT_PAUSE_BETWEEN_MESSAGES=0.8

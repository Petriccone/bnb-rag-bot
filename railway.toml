# Railway — deploy do bot SDR Telegram
# O bot usa long polling (worker), não abre porta HTTP.

[build]
builder = "nixpacks"

[deploy]
startCommand = "python run_production.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
